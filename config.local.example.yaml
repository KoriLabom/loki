# Plantilla de overrides privados. Copiar a config.local.yaml (ignorado por git)
# y completar con datos reales. No commitear el archivo resultante.

alias_proyectos:
  el tablero: nombre-del-repo-en-orca

terminos_privados:
  - ejemplo-de-termino-que-no-debe-aparecer-en-el-diff

overlay:
  posicion:
    x: 100
    y: 100
    monitor: 0

# Overrides opcionales de cualquier clave de config.yaml, por ejemplo:
# modelos:
#   cerebro: sonnet
